@model IEnumerable<mvcDatabase.ders>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ders_adi)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ders_ID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ders_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ders_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ders_ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
